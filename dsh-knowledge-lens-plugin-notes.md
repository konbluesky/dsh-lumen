# DSH「知识透镜」插件构想清单

> 目标：在 DSH Web GUI 的聊天过程中，用户可以选中当前会话里的任意文字，打开一个迷你搜索 / 问答 / 学习界面，在不打断主会话的情况下查询、理解、追问和整理知识点，并在需要时把结果带回主线。

---

## 1. 背景痛点

- 和 AI 聊天时，经常会遇到不熟悉的概念、术语、框架、论文、API 或背景知识。
- 当前常见流程是：中断主聊天 → 新开会话或浏览器搜索 → 查询学习 → 回到原会话 → 手动复制或重组上下文。
- 这个流程的问题：
  - 上下文切换成本高；
  - 主会话容易被临时问题打断；
  - 查询结果和原会话割裂；
  - 查到的信息不容易沉淀；
  - 后续复盘时，知识点散落在多个窗口或会话中。

---

## 2. 插件核心目标

设计一个基于 DSH 插件系统的会话内知识查询插件。用户可以：

1. 在当前聊天内容中选中任意文字；
2. 通过浮动按钮、右键菜单或快捷键打开迷你问答界面；
3. 针对选中的内容进行解释、搜索、追问、整理；
4. 查询过程默认不污染主会话；
5. 需要时再把结果插入输入框、保存为笔记，或作为上下文带回主线。

---

## 3. 推荐名称与定位

推荐名称：**知识透镜 / Knowledge Lens**。

其他候选：

- 上下文透镜 / Context Lens
- 会话旁查
- 支线问答
- 选区研究
- Chat Lens
- Mini Research Panel

一句话定位：

> DSH 会话内的划词式迷你研究助手。

完整定位：

> 用户在 DSH 当前聊天中选中任意文本后，可以就地打开一个轻量研究面板，结合当前会话上下文进行解释、联网搜索、支线追问和知识整理，并在需要时将结果结构化带回主会话。

---

## 4. 类似产品调研结论

### 4.1 总体结论

市面上有很多“选中文字后让 AI 解释 / 翻译 / 总结”的工具，但它们大多是浏览器级 AI 助手，主要服务于网页阅读场景。

我们要做的不是普通网页划词助手，而是：

> DSH 当前会话内的上下文知识支线系统。

### 4.2 Monica

官网：https://monica.im/

相似点：

- 支持选中文字后解释、翻译、总结；
- 有浮层或工具栏式交互；
- 适合快速理解网页内容。

差异点：

- Monica 面向整个浏览器网页；
- 不绑定 DSH 当前 session；
- 不天然理解当前 AI 会话上下文；
- 查询结果不会自动成为当前会话的结构化知识资产。

### 4.3 MaxAI

官网：https://www.maxai.me/

相似点：

- 支持选择网页文字后一键解释；
- 适合阅读增强和快速理解。

差异点：

- 面向 browser-wide assistant；
- 不理解 DSH 会话结构；
- 不强调支线查询与主线对话之间的回填闭环。

### 4.4 Sider / Merlin / ChatGPT Sidebar 类产品

常见能力：浏览器侧边栏 AI、网页划词解释、网页总结、搜索结果增强、翻译、改写、总结。

差异点：它们主要面向网页内容，不是 DSH 原生插件，不具备 DSH session-aware 的上下文整合能力，也不以“支线不污染主线”为核心设计。

### 4.5 IDE / 编辑器 Explain Selection

例如 VS Code、Cursor、JetBrains AI、Copilot Chat 等。

相似点：用户选中一段内容后问 AI，不离开当前工作区。

差异点：IDE 场景主要围绕代码；本插件围绕聊天会话中的知识理解、搜索和整理；本插件更强调查询结果和主会话之间的可控关系。

---

## 5. 差异化价值

### 5.1 会话内原生体验

插件运行在 DSH Web GUI 内部，可以读取当前会话上下文、理解选中文本所在消息、知道消息角色，并和当前输入框、session、agent、subagent 等能力联动。

### 5.2 上下文感知解释

同一个词在不同语境中含义可能完全不同。例如 “plugin”：

- 浏览器语境：Chrome extension；
- Vite 语境：Vite plugin；
- DSH 语境：Cordis plugin / client plugin；
- LLM 工具体系：model-facing tool plugin。

所以插件不应该只把选中文字孤立发给模型，而应结合当前会话解释。

### 5.3 支线不污染主线

默认行为应该是：用户可以随手查、随手追问，但支线问答默认不进入主会话。只有用户主动选择“带回主线”时，结果才进入主会话上下文。

### 5.4 查询结果可沉淀

查询结果可以变成：知识卡片、术语表、session note、学习笔记、Anki 风格问答、下一轮主会话的 context patch。

---

## 6. 核心使用场景

### 6.1 快速解释概念

用户选中“Cordis 的依赖注入机制”，插件提供：

- 一句话解释；
- 面向初学者解释；
- 结合当前 DSH 插件上下文解释；
- 类比解释；
- 给出 DSH 插件开发相关例子。

### 6.2 联网查证

用户选中某个概念、库、API、论文名，例如 RAG Fusion、Contextual Retrieval、OpenAI Responses API、Cordis plugin framework。

插件可以：

- 搜索最新资料；
- 多来源汇总；
- 给出引用链接；
- 判断信息可信度；
- 指出当前对话中的说法是否准确；
- 总结和当前话题的关系。

### 6.3 支线追问

用户在迷你窗口中继续问：

- “能再简单点吗？”
- “和 Vite plugin 有什么区别？”
- “这个概念和我们正在设计的插件有什么关系？”
- “如果我要实现它，第一步应该看哪里？”

这些追问形成 mini thread，不进入主会话。

### 6.4 回填主线

用户查完后，可以选择：复制结果、插入主输入框、生成主会话补充背景、保存为 session note、生成知识卡片、把支线摘要作为下一轮上下文。

---

## 7. 功能清单

### 7.1 触发方式

- 选中文字后显示浮动按钮；
- 右键菜单增加“用知识透镜查询”；
- 快捷键打开，例如 `Cmd/Ctrl + Shift + K`；
- 选区后直接出现轻量 action bar。

### 7.2 默认动作

建议默认动作是：**结合当前会话解释这段内容**。

原因：这是最高频需求，不一定需要联网，实现成本比搜索低，也能最直接体现 DSH 会话上下文价值。

### 7.3 查询模式

- **解释**：解释概念、术语、代码片段、架构描述；
- **搜索**：联网搜索并总结；
- **追问**：围绕选区进行多轮问答；
- **整理**：生成知识卡片、术语表、学习笔记；
- **验证**：判断当前说法是否准确、是否过时；
- **回填**：将结果插入主输入框或作为上下文补丁。

### 7.4 推荐输出格式

```markdown
## 简短结论

...

## 关键点

1. ...
2. ...
3. ...

## 和当前对话的关系

...

## 可能的误区

...

## 来源

- [source 1](...)
- [source 2](...)
```

---

## 8. 交互形态

### 8.1 浮动按钮 + Popover

流程：用户选中文字 → 选区旁出现小按钮 → 点击后打开 popover → 展示解释结果和后续操作。

优点：轻量、不打扰、适合高频快速解释。

缺点：空间有限，复杂搜索结果展示不方便，需要处理选区定位、滚动、遮挡等问题。

### 8.2 右侧 Drawer

流程：用户选中文字 → 右侧打开固定研究面板 → 支持较长回答、多轮问答和引用展示。

优点：空间更大，适合复杂研究，适合展示来源和笔记。

缺点：交互更重，占用主会话布局空间。

### 8.3 推荐形态：Popover + 可展开 Drawer

- 默认打开轻量 popover；
- 简单解释在 popover 中完成；
- 用户点击“展开研究”后变成右侧 drawer；
- drawer 中支持多轮追问、搜索、卡片和引用。

---

## 9. MVP 建议

### 9.1 MVP 一句话目标

在 DSH Web 聊天界面中，用户选中任意消息文本后，可以打开浮窗，对选中文本进行上下文解释，并能把解释结果插入主输入框。

### 9.2 MVP 必做项

- 监听聊天区文本选择；
- 获取选中文字；
- 判断选区是否位于聊天区域内；
- 在选区附近显示浮动按钮；
- 点击按钮打开 mini panel；
- 自动带入选中文字、所属消息、最近若干轮上下文；
- 默认执行“解释这段内容”；
- 展示 AI 回答；
- 支持复制；
- 支持插入主输入框；
- 支持关闭。

### 9.3 MVP 暂缓项

- 联网搜索；
- 多轮支线问答；
- 知识卡片；
- 笔记系统；
- 引用管理；
- 多模型 / 多 provider 配置；
- 支线历史记录；
- 跨 session 知识库。

---

## 10. 版本路线

### v0.1：上下文解释

划词触发、浮动按钮、Popover、基于当前会话上下文解释、支持复制和插入输入框。

### v0.2：迷你追问

支持一次或多次追问；每个选区形成独立 mini thread；支线默认不进入主会话；支持展开为 Drawer。

### v0.3：联网搜索

接入搜索能力；生成带引用摘要；支持“只查官方文档”“只查 GitHub / docs / paper”；支持来源可信度提示。

### v0.4：知识整理

生成知识卡片；保存为 session note；支持当前 session 的知识卡片列表；支持将卡片带回主线。

### v0.5：支线 Agent

使用 subagent / fork agent 承载独立研究；支持工具调用和联网搜索；支持将支线结果作为 context patch 附加给主会话；主会话只展示折叠摘要，避免噪音。

---

## 11. 技术实现方向

### 11.1 插件形态

该功能适合做成 DSH Web client plugin。

可能目录结构：

```text
packages/client/ui-knowledge-lens/
  src/index.ts
  src/client.tsx
  package.json
```

如果后续需要 host 侧能力，可以再拆分：

```text
packages/client/ui-knowledge-lens/
packages/host/knowledge-lens/
packages/tool/knowledge-lens-search/
```

### 11.2 DSH 插件体系相关事实

根据当前 DSH 文档：

- DSH 是 “everything is a plugin” 架构；
- Cordis 插件通过 `apply(ctx)` 注册能力；
- client 插件通过 `dsh.client` manifest 声明浏览器侧 bundle；
- Web client 插件作为独立 bundle 加载；
- 插件之间应通过 Cordis service / inject 协作，而不是直接 value import 其他插件。

### 11.3 需要进一步确认的 DSH 能力

后续实现前需要确认：

- 是否已有 conversation/message render slot；
- 是否已有 conversation container service；
- 是否能稳定读取当前消息列表；
- 是否能获取当前 session runtime；
- 是否有主输入框写入 API；
- 是否有 toast / popover / drawer primitive；
- 是否能从 client 侧创建 subagent / fork agent；
- 是否已有适合 session note / knowledge card 的持久化位置。

### 11.4 选区实现思路

MVP 可先使用 DOM selection：

- 监听 `selectionchange`；
- 使用 `document.getSelection()` 获取选区；
- 判断 selection anchor/focus 是否落在 conversation 容器内；
- 通过 `Range.getBoundingClientRect()` 计算浮动按钮位置；
- 保存 selected text；
- 尝试通过 DOM 找到所属 message 元信息。

长期更理想的方式：

- DSH 提供 message-level selection service；
- 插件通过 service 获取 messageId、role、content、session context；
- 避免依赖不稳定 DOM 结构。

---

## 12. 迷你问答执行方案

### 12.1 方案 A：直接发给当前 agent

优点：实现简单，复用当前会话能力。

缺点：会污染主会话，与“支线不打断主线”的目标冲突。

结论：不推荐作为默认方案。

### 12.2 方案 B：One-shot LLM 调用

优点：适合 MVP，简单快速，不需要管理支线会话生命周期。

缺点：工具调用能力较弱，联网搜索能力可能不足，和 DSH agent 体系结合较浅。

结论：推荐用于 v0.1。

### 12.3 方案 C：Subagent / Fork Agent

优点：适合真正的支线研究，可继承当前会话上下文，可使用工具，不污染主线。

缺点：UI 和生命周期管理复杂，需要处理多个支线 thread。

结论：推荐在 v0.3 或 v0.5 引入。

---

## 13. 搜索能力方案

可选方案：

1. 插件直接接搜索 API；
2. 调用 DSH 已有 web search / fetch 能力；
3. 让支线 subagent 使用已有工具进行搜索和总结。

推荐方向：

> 让支线 agent 使用 DSH 已有工具完成搜索和总结。

理由：更符合 DSH 架构；搜索、总结、引用生成可以交给 agent；UI 插件只负责选区、展示和回填；后续可扩展性更好。

---

## 14. 知识卡片设想

知识卡片可以包含：

```markdown
# Cordis 依赖注入

## 一句话
Cordis 通过 ctx 提供服务注册与依赖注入，让插件之间通过服务协作，而不是直接 import 对方实现。

## 为什么重要
它让 DSH 的功能可以被拆成很多可组合插件。

## 和当前项目的关系
我们要做的选择文本浮窗插件，很可能需要作为 DSH client plugin 注册 UI 能力，并通过已有服务读取会话数据。

## 相关概念
- client plugin
- ctx
- service inject
- session projection
- UI slot

## 待追问
- DSH 当前是否已有可用的 message selection slot？
- 插件能否直接读取当前 conversation DOM selection？
```

---

## 15. 需要继续讨论的问题

1. 第一版优先级是什么：解释选中文字、联网搜索、支线问答、知识整理？
2. 查询结果是否默认保存：默认不保存、默认保存在当前 session、用户手动保存？
3. 支线问答是否从第一版就使用 subagent：MVP 不用、搜索模式才用、从第一版就用？
4. 回填主线的形式是什么：插入输入框、追加上下文事件、折叠摘要、session note？
5. 插件更像哪类产品：浏览器划词助手、IDE Explain Selection、Perplexity mini search、Notion / Obsidian 知识卡片，还是组合？
6. 是否需要全局知识库：只在当前 session 有效、跨 session 复用、可导出 Markdown、同步外部笔记系统？
7. 是否需要权限与隐私设置：是否允许发送最近消息上下文、联网搜索、保存支线历史、进入长期记忆？

---

## 16. 推荐拍板方案

### 名称

**Knowledge Lens / 知识透镜**

### 定位

DSH 会话内的划词式迷你研究助手，帮助用户在不打断主聊天的情况下理解、搜索、追问和整理知识点。

### MVP

1. 选中聊天文字；
2. 出现浮动按钮；
3. 点击打开 popover；
4. 默认执行“结合当前会话解释”；
5. 支持一次追问或自定义问题；
6. 支持复制；
7. 支持插入主输入框。

### 后续增强

- 右侧展开为研究面板；
- 支持 mini thread；
- 支持联网搜索和引用；
- 支持知识卡片；
- 支持 subagent 支线研究；
- 支持 context patch 带回主线；
- 支持 session-level knowledge base。

---

## 17. 最终总结

现有同类产品大多是“网页 AI 划词助手”。这个 DSH 插件更应该被设计成：

> DSH 会话中的知识支线系统。

它的核心价值不是单纯解释文字，而是在不打断主线的前提下，把临时学习、联网查证、支线追问和知识整理整合进当前 AI 会话工作流。
