/**
 * DSH Lumen host half.
 *
 * This is currently a browser-only UI surface. The empty host apply keeps the
 * package loadable as a Cordis row so DSH Web can discover its dsh.client
 * manifest and serve the browser bundle exported from ./client.
 */
export function apply(): void {}
